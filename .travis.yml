sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t shaktichetan/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true shaktichetan/docker-react npm run test
